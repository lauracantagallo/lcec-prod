---
layout: base.njk
---
<section class="section section--white section--flush-bottom">
  <div class="container">
    <h1 class="why-content__heading">{{ page_title.heading }}</h1>
    <div class="why-content">
      <img src="{{ '/img/laura-cantagallo.webp' | url }}" alt="Laura Cantagallo, founder of LC Education Consulting" class="why-content__photo" width="400" height="400" />
      <div class="why-content__text">
        <p class="why-content__subheading">{{ page_title.subheading }}</p>
        {% for paragraph in body %}
        <p class="why-content__body">{{ paragraph }}</p>
        {% endfor %}
      </div>
    </div>
  </div>
</section>

<div class="container">
  <hr class="divider" />
</div>

<section class="section section--white section--flush-top">
  <div class="container">
    <div class="credentials-layout">
      <div class="credentials-layout__text">
        <h2 class="section-heading">{{ credentials_heading }}</h2>
        <ul class="credentials-list">
          {% for item in credentials %}
          <li>{{ item }}</li>
          {% endfor %}
        </ul>
      </div>
      <img src="{{ '/img/why-choose-us_career.webp' | url }}" alt="" class="credentials-layout__photo" aria-hidden="true" loading="lazy" />
    </div>
  </div>
</section>

<section class="section section--white">
  <div class="container">
    <h2 class="philosophy-section__heading">What Clients Have to Say</h2>
    <div class="philosophy-section__block why-quote">
      <p class="why-quote__text">{{ quote.text }}</p>
      <p class="why-quote__attribution">&mdash; {{ quote.attribution }}</p>
    </div>
  </div>
</section>

{% include "partials/social-section.njk" %}
